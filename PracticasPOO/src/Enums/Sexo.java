package Enums;

public enum Sexo {
    HOMBRE, MUJER, OTRO

//Así se usan los enums
    /*Sexo sexo = Sexo.HOMBRE;    // Instancia de un enum de la clase Sexo
    sexo.name();    // Devuelve un String con el nombre de la constante (HOMBRE)
    sexo.toString();    // Devuelve un String con el nombre de la constante (HOMBRE)
    sexo.ordinal();    // Devuelve un entero con la posición del enum según está declarada (3).
    sexo.compareTo(Enum otro);    // Compara el enum con el parámetro según el orden en el que están declarados lo enum
    Sexo.values(); */   // Devuelve un array que contiene todos los enum
}
